import { createContext, useContext, useReducer } from "react";

export const BusinessesContext = createContext(null);

// const initialBusinesses = [];

// export  const businessesReducer = (action) => {
//   switch(action.type) {
//     case 'add_businesses':
//       return null;
//     default:
//       return null;
//   }
// }